{
  "name": "riyadh-taraweeh",
  "version": "1.0.0",
  "description": "Taraweeh Imams Directory - Riyadh",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "heroku-postbuild": "cd frontend && npm ci && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
